#!/usr/bin/env bash

echo -en "\tFinished loading to index at "
date
echo -en "Count of inserted documents: "
docker exec clickhouse_engine clickhouse-client --query="SELECT count(*) FROM default.$test"
